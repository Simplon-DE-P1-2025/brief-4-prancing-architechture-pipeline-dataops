 # ─────────────────────────────────────────────────────────────────────
# requirements.txt — Dépendances Python du projet
# À placer à la racine du projet Astro CLI
# ─────────────────────────────────────────────────────────────────────

# HTTP
requests>=2.31.0

# PostgreSQL
psycopg2-binary>=2.9.9

# SODA — Qualité des données
soda-core>=3.3.0
soda-core-postgres>=3.3.0

# Provider Airflow officiel SODA (optionnel, pour SodaScanOperator)
# apache-airflow-providers-soda>=1.1.0

# Utilitaires
python-dotenv>=1.0.0
# Providers Airflow (pas apache-airflow lui-même, géré par Astro)
apache-airflow-providers-postgres
apache-airflow-providers-http

# Soda
soda-core-postgres==3.5.6

# Utils
requests>=2.32.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pandas==2.2.2